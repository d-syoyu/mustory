# Shared packages

This folder will host shared Dart/Python packages:

- `packages/dart_core`: shared models + utilities for the Flutter app.
- `packages/python_client`: typed API client / SDK for worker or CLI usage.

> TODO: bootstrap packages once API schemas stabilize.
